# yamllint disable
apiVersion: v1
kind: Secret
metadata:
    name: onepassword-connect-secret
    namespace: security
stringData:
    token: "{{ bootstrap_onepassword_token }}"
    1password-credentials.json: {{ bootstrap_onepassword_json }}

